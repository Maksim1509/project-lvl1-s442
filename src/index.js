import readlineSync from 'readline-sync';

export const welcome = 'Welcome to the Brain Games!';

export const request = readlineSync;

export const greeting = name => console.log(`'Hello ${name}!'`);
